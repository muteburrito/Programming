# Tuples are similar to list but they cannot be updated or modified 

t1 = (1,3,6,2,88,33,223,32)
print(t1)
#tuples can also be accessed via indexing 
print(t1[3])
# Tuples cannot be modified and it cannot be immutable
t_empty = ()
#This creates a empty tuple
t0 = (1,) # tuple with single element, a comma is important as if comma is not there then it will detect as literal
print(t0)
